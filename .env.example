# Copy to .env and fill in. .env is gitignored.
PGHOST=db.<your-project-ref>.supabase.co
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres
PGPASSWORD=your-database-password
PGSSL=require
PORT=3000
# Set to 1 when behind a reverse proxy (nginx/Caddy) for correct client IPs.
TRUST_PROXY=1
