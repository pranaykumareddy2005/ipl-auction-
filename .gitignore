# Runtime + secrets — never commit
# environment / DB credentials:
.env
.env.*
!.env.example
# team PINs + HMAC signing secret (legacy single-room):
data/access.json
# live auction state (legacy single-room event log):
data/events.log
# per-room local storage (when running file-backed):
data/rooms/
node_modules/
*.bak
*.tmp
.DS_Store
Thumbs.db
